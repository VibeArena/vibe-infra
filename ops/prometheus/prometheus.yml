global:
  scrape_interval: 15s

scrape_configs:
  # Scrape OTel Collector's Prometheus exporter (Core VM)
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel:9464']

  # (옵션) Django/Worker가 /metrics(프로메테우스 포맷)를 직접 노출한다면 추가
  # - job_name: 'django-web'
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['web:8000']
