Place TLS cert files here for nginx container:

- fullchain.pem
- privkey.pem

If you use Let's Encrypt on the host, you can mount symlinks to these filenames.
For quick testing, you can use a self-signed certificate instead.
